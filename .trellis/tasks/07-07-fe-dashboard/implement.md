# M3-2 执行计划

1. [x] `utils/geo.ts` 抽出 loadGeoJson
2. [x] `components/HeatMap.vue`（selectedMode + select 事件 + selectedName 联动）
3. [x] MapView 重构复用（回归：城市切换、点击弹窗）
4. [x] DistrictBar 增加 selectedName 高亮
5. [x] DashboardView + 路由 + 导航
6. [x] 验证：npm run type-check && npm run build；浏览器实测四图联动与 MapView 回归
7. [x] 提交、finish

## 验证命令

```bash
cd frontend && npm run type-check && npm run build
```
